(()=>{const data=window.HTFO_FILTERS||[],$=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function card(x){return `<article class="resultcard"><div><h3>${esc(x.oncore)} Replacement Filter</h3><div class="meta"><strong>OnCore:</strong> ${esc(x.oncore)}${x.unicel?` · <strong>Unicel:</strong> ${esc(x.unicel)}`:''}${x.pleatco?` · <strong>Pleatco:</strong> ${esc(x.pleatco)}`:''}<br><strong>Catalog family:</strong> ${esc(x.brand)}</div><div class="status">Exact fit details being verified for online ordering</div></div><div><div class="price">${x.price==null?'Price coming soon':`$${Number(x.price).toFixed(2)}`}</div><div class="pending">Ordering disabled until dealer pricing + fulfillment are loaded</div></div></article>`}function render(el,rows,msg){el.innerHTML=rows.length?rows.map(card).join(''):`<div class="empty">${esc(msg)}</div>`}document.querySelectorAll('.brandbtn').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.brandbtn').forEach(x=>x.classList.remove('active'));b.classList.add('active');const brand=b.dataset.brand;render($('#brandResults'),data.filter(x=>(x.shopBrands||[]).includes(brand)),`We’re loading the ${brand} filter catalog now. No unverified filters will be offered for sale.`)}));function vals(k){return [...new Set(data.map(x=>x[k]).filter(Boolean))].sort((a,b)=>parseFloat(a)-parseFloat(b))}function fill(sel,k){vals(k).forEach(v=>sel.insertAdjacentHTML('beforeend',`<option value="${esc(v)}">${esc(v)}&quot;</option>`))}fill($('#od'),'od');fill($('#length'),'length');$('#findFilter').addEventListener('click',()=>{const od=$('#od').value,len=$('#length').value,top=$('#top').value,bottom=$('#bottom').value;if(!od||!len){render($('#sizeResults'),[],'Outside diameter and length are required. Use exact measurements.');return}const rows=data.filter(x=>x.od===od&&x.length===len&&(!top||x.top===top)&&(!bottom||x.bottom===bottom));render($('#sizeResults'),rows,'No exact verified match is loaded for those measurements yet. Bring the old filter to either showroom rather than guessing.');});})();