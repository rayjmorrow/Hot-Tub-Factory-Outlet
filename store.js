(()=>{const KEY='htfo_cart_v1';const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n);const get=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}};const save=c=>{localStorage.setItem(KEY,JSON.stringify(c));renderBadge()};const add=p=>{const c=get();const hit=c.find(x=>x.id===p.id);if(hit)hit.qty+=1;else c.push({...p,qty:1});save(c);toast(`${p.name} added to cart`)};const parsePrice=t=>{const m=(t||'').replace(/,/g,'').match(/\$([0-9]+(?:\.[0-9]{1,2})?)/);return m?Number(m[1]):null};function itemHtml(sku,name,price){return `<div class="item"><b>${name}</b><span>${sku}</span><em>This Week’s Sale Price</em><strong>${money(price)}</strong></div>`}function cardHtml(sku,name,desc,price){return `<article class="card"><div class="body"><h3>${name}</h3><p>${desc}</p><div class="sku">${sku}</div><div class="sale">This Week’s Sale Price</div><div class="price">${money(price)}</div></div></article>`}function completeCatalog(){const chem=document.getElementById('chemgrid');if(chem&&!chem.textContent.includes('TSAC103'))chem.insertAdjacentHTML('beforeend',itemHtml('TSAC103','Total Hardness Test Strips',17.99)+itemHtml('TSAC104','TDS Test Strips',17.99));const acc=document.getElementById('accgrid');if(acc){const sediment=[...acc.querySelectorAll('.item')].find(x=>x.querySelector('span')?.textContent.trim()==='CHSE300');if(sediment){const name=sediment.querySelector('b'),price=sediment.querySelector('strong');if(name)name.textContent='Sediment Pre-Filter';if(price)price.textContent='$14.99'}if(!acc.textContent.includes('Sediment Filter'))acc.insertAdjacentHTML('beforeend',itemHtml('CHSE300','Sediment Filter',12.99))}const pdf=document.querySelector('img[src$=".pdf"]');if(pdf)pdf.closest('.pic')?.remove();const lifters=document.querySelector('#lifters .grid');if(lifters&&!lifters.textContent.includes('VSL400'))lifters.insertAdjacentHTML('beforeend',cardHtml('VSL400','VacuSeal 400 Cover Lifter','Premium VacuSeal-compatible cover lifter.',599)+cardHtml('VSLA201','VacuSeal 200 → 400 Conversion Kit','Conversion kit for compatible VacuSeal lifter systems.',249)+cardHtml('CLP100SS','Swim Spa ConvertALift','Powered cover-lifter solution for compatible swim spas.',2795)+cardHtml('CLP100SS-UG','Hot Tub → Swim Spa ConvertALift Upgrade','Upgrade package for compatible ConvertALift installations.',1299));const steps=document.querySelector('#steps .grid');if(steps&&!steps.textContent.includes('RADM036'))steps.insertAdjacentHTML('beforeend',cardHtml('RADM036','Deck-Mount Rail · up to 22 in','Compact deck-mount safety rail for lower spa installations.',189));const coverSkus={'Economy 4 × 2':'CVTH100','Bronze 4 × 2.5':'CVTH200','Silver 4 × 2.5':'CVTH300','Gold 5 × 3':'CVTH400','Platinum 6 × 4':'CVTH500'};document.querySelectorAll('#covers .card').forEach(c=>{const h=c.querySelector('h3');if(!h||!coverSkus[h.textContent.trim()]||c.querySelector('.sku'))return;const sku=document.createElement('div');sku.className='sku';sku.textContent=coverSkus[h.textContent.trim()];h.after(sku)})}function productFrom(el){const name=(el.querySelector('h3,b')?.textContent||'').trim();const sku=(el.querySelector('.sku,span')?.textContent||name).trim();const price=parsePrice(el.querySelector('.price,strong')?.textContent);if(!name||price===null)return null;return{id:`${sku||name}|${name}`,name,sku,price}}function buttonFor(el,p){if(el.querySelector('.add-cart'))return;const b=document.createElement('button');b.className='add-cart';b.type='button';b.textContent='Add to Cart';b.addEventListener('click',()=>add(p));const host=el.querySelector('.body')||el;host.appendChild(b)}function decorate(){document.querySelectorAll('.card,.item').forEach(el=>{const p=productFrom(el);if(!p)return;if(p.name==='VacuSeal Covers')return;buttonFor(el,p)});const vac=[['VSCH096','VacuSeal Cover · up to 96 × 96',899],['VSCH108','VacuSeal Cover · up to 96 × 108',1399],['VSCH138','VacuSeal Tri-Fold Cover · up to 138 in',1899],['VSCH192','VacuSeal Swim Spa Cover · 11–16 ft',2599],['VSCH228','VacuSeal Swim Spa Cover · 194–228 in',2999],['VSCH269','VacuSeal Swim Spa Cover · 229–269 in',3799]];const card=[...document.querySelectorAll('.card')].find(x=>x.querySelector('h3')?.textContent.trim()==='VacuSeal Covers');if(card&&!card.querySelector('.vac-options')){const box=document.createElement('div');box.className='vac-options';box.innerHTML=vac.map(v=>`<button type="button" data-id="${v[0]}|${v[1]}" data-sku="${v[0]}" data-name="${v[1]}" data-price="${v[2]}"><span>${v[1]}</span><b>${money(v[2])}</b></button>`).join('');box.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;add({id:b.dataset.id,sku:b.dataset.sku,name:b.dataset.name,price:Number(b.dataset.price)})});card.querySelector('.body')?.appendChild(box)}}function renderBadge(){const qty=get().reduce((s,x)=>s+x.qty,0);document.querySelectorAll('.cart-count').forEach(x=>x.textContent=qty)}function chrome(){const a=document.createElement('a');a.href='cart.html';a.className='cart-fab';a.setAttribute('aria-label','View shopping cart');a.innerHTML='Cart <span class="cart-count">0</span>';document.body.appendChild(a);const s=document.createElement('style');s.textContent=`.add-cart{margin-top:14px;width:100%;border:0;border-radius:10px;background:#0b79b7;color:#fff;font-weight:900;padding:12px 14px;cursor:pointer;font-size:15px}.add-cart:hover{filter:brightness(.94)}.cart-fab{position:fixed;right:18px;bottom:18px;z-index:9999;background:#082642;color:white;text-decoration:none;border-radius:999px;padding:13px 17px;font-weight:900;box-shadow:0 8px 30px #0004}.cart-count{display:inline-grid;place-items:center;min-width:24px;height:24px;padding:0 6px;margin-left:5px;border-radius:999px;background:#f5b82e;color:#082642}.vac-options{display:grid;gap:8px;margin-top:13px}.vac-options button{display:flex;justify-content:space-between;gap:12px;align-items:center;text-align:left;border:1px solid #dce6eb;background:#f8fbfc;border-radius:10px;padding:10px;cursor:pointer;color:#082642}.vac-options span{font-size:12px}.vac-options b{white-space:nowrap}.store-toast{position:fixed;left:50%;bottom:80px;transform:translateX(-50%);z-index:10000;background:#082642;color:#fff;border-radius:10px;padding:10px 15px;font-weight:800;box-shadow:0 6px 24px #0004}`;document.head.appendChild(s)}function toast(t){let x=document.querySelector('.store-toast');if(!x){x=document.createElement('div');x.className='store-toast';document.body.appendChild(x)}x.textContent=t;clearTimeout(x._t);x._t=setTimeout(()=>x.remove(),1500)}document.addEventListener('DOMContentLoaded',()=>{chrome();completeCatalog();decorate();renderBadge()});window.HTFOCart={get,save,add,money};})();